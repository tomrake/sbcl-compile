#!/usr/bash
echo "BOOTING_COMPILER=${BOOTING_COMPILER}"
echo "BOOTING_COMPILER_OPTIONS=${BOOTING_COMPILER_OPTIONS}"
SBCL_MAKE_LINE="sh make.sh '--xc-host=${BOOTING_COMPILER} ${BOOTING_COMPILER_OPTIONS}' ${SBCL_BUILD_OPTIONS}"
cd ${RELEASE_VERSION}
tar -xf sbcl-${RELEASE_VERSION}-source.tar.bz2
cd sbcl-${RELEASE_VERSION}
echo "SBCL_MAKE_LINE=${SBCL_MAKE_LINE}"
${SBCL_MAKE_LINE}

